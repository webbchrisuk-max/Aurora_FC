(() => {
  'use strict';
  const BUILD='20260827-registration-chairman-handoff-1';
  const money=v=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP',minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(v||0));
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[ch]));
  function currentRoute(state){const id=state.transfer?.activeChairmanReplacementRouteId;const rows=Array.isArray(state.transfer?.chairmanReplacementRoutes)?state.transfer.chairmanReplacementRoutes:[];return rows.find(r=>r.id===id)||rows.find(r=>r.status==='READY_FOR_REGISTRATION')||null}
  function ensure(){if(document.getElementById('chairmanRegistrationHandoff'))return document.getElementById('chairmanRegistrationHandoff');const anchor=document.querySelector('header.registration-hero');if(!anchor)return null;const section=document.createElement('section');section.id='chairmanRegistrationHandoff';section.className='department-section';anchor.insertAdjacentElement('afterend',section);return section}
  function render(){const A=window.AuroraClean;if(!A)return;const host=ensure();if(!host)return;const route=currentRoute(A.readState());if(!route){host.hidden=true;host.innerHTML='';return}host.hidden=false;const sale=route.sale||{},replacement=route.replacement||{};host.innerHTML=`<div class="section-heading"><div><p class="eyebrow">CHAIRMAN REPLACEMENT ROUTE</p><h2>Income recycling waiting for execution</h2></div><span class="stage-badge">${esc(route.status||'READY')}</span></div><div class="scouting-control-grid"><article class="scouting-control-card"><span>SELL</span><strong>${esc(sale.ticker||'—')}</strong><small>${esc(sale.account||'')} · ${Number(sale.shares||0).toLocaleString('en-GB',{maximumFractionDigits:6})} shares · proceeds ${money(sale.proceeds)}</small></article><article class="scouting-control-card scouting-top-card"><span>REPLACE WITH</span><strong>${esc(replacement.ticker||'—')}</strong><small>${esc(replacement.account||'')} · ${money(replacement.amount)} planned</small></article><article class="scouting-control-card"><span>INCOME UPLIFT</span><strong>+${money(replacement.incomeUplift)}/yr</strong><small>+${Number(replacement.incomeUpliftPct||0).toFixed(1)}% · ${esc(replacement.confidence||'')}</small></article></div><p>This route came from an accepted Chairman's Offer. It is visible here for execution control; the existing mission registration flow remains separate until the sale and replacement trades are confirmed.</p>`}
  function boot(){if(!window.AuroraClean){setTimeout(boot,60);return}render();window.addEventListener('aurora-clean:state',render);window.AuroraRegistrationChairmanHandoff=Object.freeze({BUILD,render})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();