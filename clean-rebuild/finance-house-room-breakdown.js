(()=>{const A=()=>window.AuroraClean;const n=v=>Number(v)||0;const m=v=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP'}).format(n(v));function render(){if(!A()?.readState)return;const entries=A().readState().finance?.houseProject?.entries||[];document.querySelectorAll('#houseRoomCards .house-room-card').forEach(card=>{const room=card.querySelector('.finance-pot-card-head strong')?.textContent?.trim();if(!room)return;card.querySelector('.house-room-breakdown')?.remove();const rows=entries.filter(e=>e.status!=='archived'&&(e.room||'Whole House')===room);const estimated=rows.reduce((s,e)=>s+n(e.estimated),0),actual=rows.reduce((s,e)=>s+n(e.actual),0),reserved=rows.filter(e=>e.status==='reserved').reduce((s,e)=>s+n(e.estimated),0);const d=document.createElement('details');d.className='house-room-breakdown';const sum=document.createElement('summary');sum.textContent=`Payment breakdown · ${rows.length} items · ${m(Math.max(0,estimated-actual))} left`;d.appendChild(sum);rows.forEach(e=>{const x=document.createElement('div');x.className='house-room-payment';x.textContent=`${e.name||'House payment'} — estimated ${m(e.estimated)} — ${e.status==='reserved'?'reserved '+m(e.estimated):'paid '+m(e.actual)}`;d.appendChild(x)});const t=document.createElement('div');t.className='house-room-total';t.textContent=`Room totals: ${m(estimated)} estimated · ${m(actual)} paid · ${m(reserved)} reserved.`;d.appendChild(t);const a=card.querySelector('.finance-manage-actions');a?card.insertBefore(d,a):card.appendChild(d)})}function boot(){if(!A()){setTimeout(boot,60);return}const s=document.createElement('style');s.textContent='.house-room-breakdown{margin-top:12px;border-top:1px solid rgba(255,255,255,.08);padding-top:10px}.house-room-breakdown summary{cursor:pointer;font-weight:900}.house-room-payment{padding:8px 0;border-bottom:1px solid rgba(255,255,255,.05)}.house-room-total{padding-top:8px;color:#8da1b4}';document.head.appendChild(s);render();window.addEventListener('aurora-clean:state',()=>setTimeout(render,0));document.addEventListener('click',e=>{if(e.target.closest?.('[data-tab="house"]'))setTimeout(render,100)})}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot()})();